# Configuración del servidor
PORT=3000
NODE_ENV=development

# Configuración de JWT
JWT_SECRET=tu-jwt-secret-super-seguro-aqui

# Configuración de CORS
FRONTEND_URL=http://localhost:3000

# Configuración de base de datos (Prisma)
# Formato: postgresql://usuario:password@host:puerto/nombre_base_datos
# Ejemplo para PostgreSQL local:
DATABASE_URL=postgresql://postgres:tyson26jj@localhost:5432/hotel_db?schema=public
# Para SQLite (desarrollo/testing):
# DATABASE_URL=file:./dev.db
# Para PostgreSQL con variables separadas, puedes usar:
# DATABASE_URL=postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}?schema=public

# Configuración de Redis (para futuras implementaciones)
# REDIS_URL=redis://localhost:6379

# Configuración de logging
LOG_LEVEL=info
# Configuración de rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100


# Configuración de base de datos (para futuras implementaciones)
# DATABASE_URL=postgresql://usuario:password@localhost:5432/hotel_db
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=hotel_db
# DATABASE_USER=usuario
# DATABASE_PASSWORD=password

